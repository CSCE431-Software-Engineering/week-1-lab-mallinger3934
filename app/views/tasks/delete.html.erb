<%= link_to("Back to Homepage", tasks_path, class: 'back-link') %>

<div class="tasks delete">
    <h2>Delete Book</h2>

    <%= form_for(@book, url: task_path(@book), method: 'delete') do |f| %>
        <% # url: task_path(@book), method: 'delete' %>

        <p>Are you sure you want to permanently delete this task?</p>
        <p><%= @book.name %></p>

        <div class="form-buttons">
            <%= f.submit("Delete Task") %>
        </div>
    
    <% end %>
</div>